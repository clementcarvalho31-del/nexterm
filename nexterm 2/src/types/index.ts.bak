// Re-export canonical types — single source of truth
// This file lives at src/types/index.ts
// Import via: import type { ... } from '@/src/types'

export type {
  Nullable,
  TickData, CurrencyPair, OHLCBar,
  WSMessageType, WSMessage, ConnectionStatus,
  TabId, Tab,
  NewsImpact, NewsItem,
  CalendarEvent,
  COTPosition,
  UserRole, SubscriptionStatus, TerminalUser,
  PanelId, GridPosition, PanelConfig, WorkspaceLayout,
  Timeframe, ChartEngineType, ChartThemeId, IndicatorConfig, ChartInstanceConfig,
  ChatMessage,
} from './types'

export { hasAccess } from './types'
